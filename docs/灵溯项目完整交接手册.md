# 灵溯项目完整交接手册

> 最后核验日期：2026-08-31（Asia/Shanghai）  
> 交接性质：原负责人彻底退出，接手者需能独立维护  
> 线上服务器：`ubuntu@1.15.230.239`  
> 本地项目目录：`/Users/anzhi/Desktop/雷灵/星辰/lingsu`  
> 证据标记：**已实测** / **源码确认** / **文档记录** / **待确认**

> [!CAUTION]
> 线上环境正在承载真实业务数据。本地代码显著领先于线上运行镜像，严禁直接覆盖、迁移或重建生产环境。本文不保存密码、API Key、私钥或数据库明文凭据。

## 1. 交接首页

### 1.1 一句话说明

灵溯是一套面向学校的青少年科学创新协作平台。学生完成“开题—项目—任务—材料—报告—成果展示”，教师负责认领、指导和审核，平台管理员负责学校授权、赛事、公告、案例和 AI 配置治理。

### 1.2 当前结论

| 事项 | 当前状态 | 证据 |
| --- | --- | --- |
| 线上业务 | 正在使用真实数据 | 用户确认，2026-08-31 |
| 线上核心服务 | PostgreSQL、Redis、Django、Celery、Nginx 在线，容器重启次数均为 0 | **已实测** |
| 线上受限能力 | 附件上传与 PDF 导出均关闭 | **已实测**：线上 `/api/health/` |
| 本地环境 | 8 个服务在线，附件和 PDF 能力开启 | **已实测** |
| 本地质量 | 前端 325 个单测通过；后端 253 个测试通过、1 个跳过；构建通过 | **已实测** |
| 浏览器 E2E | 当前不是全绿基线；观察到 6 组明确失败，运行超过 5 分钟仍未收敛后人工终止 | **已实测** |
| 最大风险 | 线上工作区、线上运行镜像、本地代码三者版本不一致 | **已实测** |
| 备份 | 仓库有脚本，但线上无 `/backups` 目录、无业务备份定时任务 | **已实测**，P0 |
| HTTPS | Nginx 只监听 80，未发现域名或 TLS 配置 | **已实测**，P0 |

### 1.3 三个版本基线

| 层次 | 版本 | 说明 |
| --- | --- | --- |
| 线上运行后端镜像 | `3dde0b69066ff91bcec30677a7e035e364117522` | 实际 Django/Celery 运行代码；数据库迁移只到 `0039` |
| 线上运行 Web 镜像 | `ae6c18e2cab453f0308ee252039e419f2b42cf5a` | 实际 Nginx 静态前端 |
| 线上服务器工作区 | `620aa599bc224bb0a9e9ff72d8fe89a2ac0a3cab` | 分支 `codex/ui-audit-consistency`，另有未跟踪生产 override 文件 |
| 本地交接基线 | `170b4cb` 及本交接文档之后的提交 | 比线上工作区多 30 余个提交，比运行镜像差距更大 |

**重要解释：**“服务器目录里有新代码”不代表“线上已经运行新代码”。生产容器仍使用旧镜像；判断线上功能必须以运行镜像和数据库迁移为准。

### 1.4 联系人与所有权

代码无法确认服务器、域名、云账号、GitHub 组织和 AI 服务的最终所有者。正式退出前必须完成第 13 节资产移交表；所有“待确认”项必须指定新责任人。

## 2. 接手者第一天

### 2.1 命令风险标签

- `[只读]`：查询状态，不应改变服务或业务数据。
- `[变更]`：会改变本地或线上状态，执行前必须理解影响。
- `[高风险]`：涉及生产发布、迁移、恢复、删除或凭据，必须先备份并获得新负责人批准。

### 2.2 先做这五件事

1. 确认已接收服务器 SSH、GitHub 仓库和云平台管理权限。
2. 登录线上三类角色，确认学生、教师和平台管理入口可用。
3. 只读检查线上容器，不重启：

   ```bash
   # [只读]
   ssh ubuntu@1.15.230.239 'cd /home/ubuntu/lingsu && docker compose ps'
   ```

   正常现象：`backend`、`postgres`、`redis` 显示 healthy；`celery`、`nginx` 显示 Up。

4. 检查线上健康接口：

   ```bash
   # [只读]
   ssh ubuntu@1.15.230.239 'curl -fsS http://127.0.0.1/api/health/'
   ```

   当前预期：`status=ok`，但 `attachments=false`、`pdf_export=false`。

5. 完成第 13、14 节签收；在备份得到验证以前，不安排生产升级。

### 2.3 绝对不要做

- 不要运行 `docker compose down -v`；`-v` 会删除数据库和媒体卷。
- 不要把本地 `.env` 上传覆盖线上 `.env`。
- 不要在生产执行 `seed_demo --reset`、`flush`、`restore.sh` 或手工删除数据。
- 不要直接运行本地最新镜像；本地有两个生产尚未应用的迁移。
- 不要通过数据库客户端编辑业务状态；状态流转包含权限、通知和审计逻辑。
- 不要把线上 `.env`、数据库备份或媒体文件提交到 Git。

### 2.4 本地快速检查

```bash
cd /Users/anzhi/Desktop/雷灵/星辰/lingsu

# [只读]
docker compose --env-file .env.integration ps
curl -fsS http://127.0.0.1:18001/api/health/
curl -fsSI http://127.0.0.1:5173/
```

本次实测：本地 8 个容器运行，后端和前端均返回 200。

## 3. 产品与业务全景

### 3.1 三类角色

| 角色 | 主要工作 | 关键限制 |
| --- | --- | --- |
| 学生 | 开题、建立项目、完成任务和材料、使用 AI、邀请成员、生成报告、申请成果展示 | 正式提交通常由项目负责人完成；只能访问本人参与项目和本校数据 |
| 教师 | 查看项目池、认领项目、选择模板、审核材料、确认成员、管理校本模板与公告、查看指导项目 AI 记录 | 只能管理本人指导项目；不得跨校访问 |
| 平台管理员 | 学校授权、配额、赛事、平台公告、公开案例、全局 AI Skills 与平台 AI 配置 | 不应访问学校内部项目、成员、材料和 AI 内容 |

前端入口：

- 学生：`/student`
- 教师：`/teacher`
- 平台管理员：`/platform`

### 3.2 项目主流程

```text
学生开题/确认开题产物
        ↓
创建待认领项目（UNCLAIMED）
        ↓
教师认领并套用模板（ACTIVE）
        ↓
生成项目任务和材料要求
        ↓
学生写草稿 → 负责人确认真实性并提交
        ↓
教师通过 / 打回修订
        ↓
任务闭环、报告导出、成果展示申请
```

### 3.3 主要状态机

- 项目：`unclaimed → active → completed`
- 任务：`available → pending_review → approved/completed`，打回为 `revision_required`
- 材料/修订：`draft → submitted → approved`，打回为 `revision_required`
- 成员邀请：`pending_student → pending_teacher → approved`，任一环节可拒绝
- 公开案例：学生校内申请或教师全平台邀请；全平台路径要求学生同意和平台审核
- AI 生成：`queued → processing → completed/failed`
- 报告导出：`queued → processing → completed/failed`

### 3.4 AI 的产品边界

AI 是可追溯的辅助工具，不应替学生编造数据或自动提交。系统保存输入、上下文范围、引用来源、生成结果、核验项和采用到材料的关联。学生/教师对话分为开题、研究、答辩工作区。

本地最新版新增平台 AI 密钥配置，使用加密字段保存；该能力依赖迁移 `0041_platform_ai_configuration`，生产尚未具备。

## 4. 当前真实状态

### 4.1 已完成并有较强证据的能力

- 三角色登录、路由守卫和按角色导航。
- 按学校隔离项目、材料、成员、通知和 AI 数据。
- 项目认领、任务生成、材料提交/审核、成员邀请/确认。
- 校内与全平台成果展示审核链路。
- AI 对话、生成日志、来源记录和材料采用留痕。
- 大文件分片上传、SHA-256、扫描状态和文档文本抽取的数据结构与任务。
- 报告 DOCX/PDF 导出任务模型。
- GitHub Actions 前端单测/构建与后端检查/迁移/测试门禁。
- 按提交 SHA 发布后端与 Web 镜像到 GHCR。

### 4.2 生产环境明确受限

- `ATTACHMENT_UPLOADS_ENABLED=0`：用户无法走附件上传入口。
- `PDF_EXPORT_ENABLED=0`：PDF 导出不可用。
- 未运行 ClamAV 和 Gotenberg。
- 未运行 Celery Beat；核心业务当前不依赖周期任务。
- 仅 HTTP 80，未看到 HTTPS 终止。
- 数据库仅 11MB、迁移记录 57 条；这些只是规模元数据，不代表数据不重要。

### 4.3 主要技术债

| 风险 | 现状 | 优先级 |
| --- | --- | --- |
| 生产备份未落地 | 脚本存在，但无 `/backups`，无 cron/systemd 业务备份 | P0 |
| 三重版本漂移 | 服务器 Git、后端镜像、Web 镜像、本地 HEAD 不一致 | P0 |
| HTTPS 未配置 | Nginx `server_name _`、只监听 80 | P0 |
| E2E 非全绿 | 25 项测试中观察到 6 组失败；包含并发重置测试数据和过期 UI 断言 | P1 |
| 后端单文件过大 | `views.py` 1726 行、`tasks.py` 697 行 | P1 |
| 前端包过大 | 主入口压缩前 928.68KB，超过 500KB 警戒线 | P1 |
| 可选服务未启用 | 附件/PDF 在生产关闭 | P1，按业务需要启用 |
| 运维观察不足 | 无项目级监控、告警和备份成功通知证据 | P1 |

### 4.4 旧文档纠偏

`docs/项目分析-2026-08-18.md` 中“无 Git、无 E2E、无 CI”的判断已经失效。当前已有 Git、4 个 Playwright spec 和 2 个 GitHub Actions 工作流。旧报告只能用来理解历史，不能作为当前验收依据。

## 5. 系统架构与代码地图

### 5.1 运行架构

```text
浏览器
  │
  ├─ 生产：Nginx :80 ── /api,/admin ── Django/Gunicorn :8000
  │                                  │
  │                                  ├─ PostgreSQL（业务数据）
  │                                  └─ Redis（Celery 队列）
  │                                         │
  │                                      Celery worker
  │
  └─ 本地：Vite :5173 ──代理── Django :18001
                                 ├─ PostgreSQL / Redis / Celery
                                 ├─ ClamAV（文件扫描）
                                 └─ Gotenberg（DOCX→PDF）
```

### 5.2 技术栈

- 前端：Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus。
- 后端：Python 3.12、Django 5、Django REST Framework、Gunicorn。
- 数据：PostgreSQL 16、Redis 7。
- 异步：Celery worker，Celery Beat 可选。
- 文件：Django 私有媒体存储，可切换 S3/MinIO；ClamAV 可选。
- 文档：python-docx 生成 DOCX，Gotenberg 转 PDF。
- 部署：Docker Compose、Nginx、GHCR、systemd 控制台。

### 5.3 关键目录

| 路径 | 职责 |
| --- | --- |
| `frontend/src/router.ts` | 三角色路由和入口 |
| `frontend/src/pages/` | 页面；按 student/teacher/platform/shared 分组 |
| `frontend/src/components/` | 通用组件和 AI 工作区组件 |
| `frontend/src/stores/` | 状态、权限和交互模型 |
| `frontend/e2e/` | Playwright 核心流程和视觉测试 |
| `backend/apps/core/models.py` | 核心数据模型 |
| `backend/apps/core/views.py` | DRF API 编排；当前过大 |
| `backend/apps/core/workflows/` | 项目、材料、成员、案例、AI 业务规则 |
| `backend/apps/core/tasks.py` | AI、扫描、抽取、报告等异步任务 |
| `backend/apps/core/tests/` | 后端权限、状态机、生产契约测试 |
| `backend/apps/core/migrations/` | 数据库迁移，当前到 0041 |
| `docker-compose.yml` | 本地与生产服务定义 |
| `deploy/` | 2GB 生产部署说明、环境模板、systemd unit |
| `scripts/backup.sh` | 数据库、媒体、环境快照备份 |
| `scripts/restore.sh` | 覆盖式恢复，高风险 |
| `.github/workflows/` | CI 质量门禁和镜像发布 |

### 5.4 API 总览

主要资源：projects、project-tasks、materials、material-revisions、attachments、upload-sessions、templates、public-case-requests、ai-logs、ai-conversations、ai-agents、report-exports、competitions、announcements、schools、member-invitations、notifications。

公共/会话端点包括：`health`、`service-status`、`me`、登录/登出、注册、改密、AI 可用性和平台 AI 配置。

## 6. 数据与权限

### 6.1 核心模型

- 组织：School、Account。
- 项目：Project、ProjectTask、ProjectMember、MemberInvitation、ProjectGrowth。
- 材料：Material、MaterialRevision、MaterialAttachment、UploadSession、UploadPart。
- 内容：Template、Competition、Announcement、PublicCaseRequest。
- AI：AgentTemplate、AIConversation、AIConversationMessage、AIGenerationLog、PlatformAIConfiguration。
- 系统：Notification、AuditEvent、ReportExport。

### 6.2 不可破坏的权限规则

1. 所有学校业务对象必须按当前用户学校过滤。
2. 学生只能访问自己领导或加入的项目。
3. 教师只能管理本人指导项目；认领时使用事务锁避免竞争。
4. 平台管理员不能读取学校项目成员、材料和 AI 过程数据。
5. 正式材料提交必须由项目负责人执行，并确认真实性。
6. 材料审核必须由主指导教师执行并留下意见。
7. AI 输出保存为材料时必须保留生成记录与材料修订的关联。
8. 附件下载必须经过 Django 权限检查，不能改成公共媒体 URL。
9. 全平台案例发布必须保留学生同意和平台审核记录。

### 6.3 数据持久化

生产命名卷：`postgres_data`、`media_data`、`redis_data`。PostgreSQL 是业务事实源；媒体卷存储私有附件；Redis 主要用于队列，不应被当作永久业务数据库。

生产部署、恢复或服务器迁移必须同时处理 PostgreSQL 与 `media_data`。只备份数据库会导致附件记录存在但文件丢失。

## 7. 本地测试环境

### 7.1 当前启动方式

```bash
cd /Users/anzhi/Desktop/雷灵/星辰/lingsu

# [变更] 启动/更新本地容器，不得在生产服务器照抄
docker compose --env-file .env.integration --profile dev up --build -d

# [只读]
docker compose --env-file .env.integration ps
```

访问：

- 前端：`http://127.0.0.1:5173`
- 后端健康：`http://127.0.0.1:18001/api/health/`
- 独立控制台：运行 `./scripts/console.sh` 后访问 `http://127.0.0.1:8800`

### 7.2 演示/测试数据

```bash
# [变更] 只允许本地测试数据库
docker compose --env-file .env.integration exec -T backend python manage.py seed_demo
docker compose --env-file .env.integration exec -T backend python manage.py seed_ai_agents
```

带 `--reset` 会重建相应演示数据，不能在生产执行。演示账号信息见 `docs/项目运行说明.md`；接手后应更换任何仍在使用的默认口令。

### 7.3 验证命令与本次结果

```bash
# 前端
cd frontend
npm test
npm run build

# 后端
cd ..
docker compose --env-file .env.integration exec -T backend python manage.py check
docker compose --env-file .env.integration exec -T backend python manage.py makemigrations --check --dry-run
docker compose --env-file .env.integration exec -T backend python manage.py test apps.core.tests --keepdb
```

2026-08-31 实测：

- 前端：67 个文件、325 个测试全部通过。
- 后端：253 个测试通过，1 个跳过；系统检查无问题；无待生成迁移。
- 构建：通过；存在包体积警告。
- E2E：25 个测试启动，观察到 6 组失败；运行超过 5 分钟仍未收敛后人工终止。已确认两类根因：多个 worker 并发执行 `seed_core_e2e --reset` 产生冲突，以及导航/AI 历史入口的断言落后于当前界面。不可宣称 E2E 全绿。

后端首次测试若提示 `test_lingsu_integration` 已存在，使用 `--keepdb`。不要为了消除提示去删除生产或集成数据库。

## 8. 线上生产环境

### 8.1 主机与资源

- 主机名：`VM-0-12-ubuntu`
- 系统：Ubuntu，Linux 5.15，x86_64。
- 系统盘：50GB，已用 15GB，可用约 33GB（31%）。
- 内存：约 1.9GiB；已配置 2GiB swap。
- 连续运行：核验时约 3 天 22 小时。
- 部署目录：`/home/ubuntu/lingsu`
- systemd：`lingsu.service`、`lingsu-console.service` 均 enabled/active。

### 8.2 生产容器

| 服务 | 当前状态 | 备注 |
| --- | --- | --- |
| backend | healthy，0 重启 | 仅绑定宿主 `127.0.0.1:8000` |
| celery | running，0 重启 | 单 worker |
| nginx | running，0 重启 | 对外 80 |
| postgres | healthy，0 重启 | 无宿主端口暴露 |
| redis | healthy，0 重启 | 无宿主端口暴露 |

### 8.3 生产功能开关

当前健康接口返回附件和 PDF 均关闭。生产未运行 ClamAV、Gotenberg、Celery Beat 和 Vite 开发容器。这是 2GB 小规格部署的主动取舍，不是偶发故障。

### 8.4 生产只读检查

```bash
# [只读] 容器状态
ssh ubuntu@1.15.230.239 'cd /home/ubuntu/lingsu && docker compose ps'

# [只读] 健康状态
ssh ubuntu@1.15.230.239 'curl -fsS http://127.0.0.1/api/health/'

# [只读] 磁盘和内存
ssh ubuntu@1.15.230.239 'df -h; free -h; swapon --show'

# [只读] 最近日志
ssh ubuntu@1.15.230.239 'docker logs --since 30m --tail 200 lingsu-backend-1'
```

日志中可能含用户输入或请求信息，禁止整段复制到群聊、工单或公开仓库。

### 8.5 凭据存放

线上敏感配置位于 `/home/ubuntu/lingsu/.env`，只应由服务器管理员读取，权限应为 600。文档只确认存在以下类别，不记录值：Django 密钥、数据库口令、AI Key、允许域名、CSRF 来源、镜像标签和服务开关。

本地最新版还需要 `AI_CONFIG_ENCRYPTION_KEY`；生产升级到迁移 0041 前必须生成、备份并安全保存该密钥，否则平台保存的 AI 配置无法解密。

## 9. 线上与本地差异

### 9.1 差异摘要

本地相对线上服务器工作区有 92 个文件变化、约 7017 行新增和 565 行删除。主要包括：

- AI 对话工作室和交互重构。
- 学生内容库与公开申请合并。
- 学生成员邀请流程完善。
- 平台 AI 配置和加密密钥。
- 两个新增数据库迁移：0040、0041。
- 大量前端布局、导航、视觉契约和测试更新。

实际运行镜像比服务器工作区还旧，所以部署差异不能只用 `620aa59..HEAD` 衡量。

### 9.2 迁移差异

生产应用到 `core.0039_aigenerationlog_workspace_mode`；本地另有：

- `0040_backfill_ai_conversation_titles`：回填 AI 对话标题。
- `0041_platform_ai_configuration`：新增平台 AI 配置表/字段及加密配置能力。

升级前必须在生产备份的隔离副本上验证迁移耗时、回填结果和旧版本回滚兼容性。

### 9.3 配置差异

本地最新版新增 `AI_CONFIG_ENCRYPTION_KEY`。生产环境变量清单中尚无该项。生产附件/PDF 关闭，本地集成环境开启。两边不能共用 `.env`。

### 9.4 结论

当前不具备“直接发布本地 HEAD”的安全条件。最低前置条件：

1. 先形成可验证的数据库和媒体备份。
2. 给当前生产镜像打可回滚标识并保存部署清单。
3. 用生产数据的脱敏/隔离副本验证 0040、0041。
4. 配置并备份 `AI_CONFIG_ENCRYPTION_KEY`。
5. 修复或明确 E2E 失败原因，至少完成三角色主流程烟雾测试。
6. 安排维护窗口和业务通知。

## 10. 安全发布与回滚

### 10.1 发布前清单

- [ ] 新负责人批准变更窗口。
- [ ] 数据库备份能被 `pg_restore --list` 读取。
- [ ] 媒体归档存在且大小合理。
- [ ] 备份已复制到服务器之外的加密存储。
- [ ] 记录当前后端/Web 镜像 SHA。
- [ ] 新镜像与代码提交一一对应。
- [ ] 在隔离环境完成迁移和烟雾测试。
- [ ] 准备旧镜像标签与数据库恢复点。
- [ ] 明确回滚负责人和允许停机时间。

### 10.2 建议发布顺序

以下均为 `[高风险]`，这里只描述顺序，不授权直接执行：

1. 冻结写入或进入维护窗口。
2. 执行并异地保存数据库、媒体和环境快照。
3. 拉取固定 SHA 镜像，不使用浮动 `latest`。
4. 先执行迁移评估，再应用迁移。
5. 更新 backend、celery、nginx，不重建 PostgreSQL 卷。
6. 检查迁移、健康接口、登录和三角色核心流程。
7. 观察日志与资源，确认无持续异常后结束维护窗口。

### 10.3 回滚触发条件

- 健康接口失败或容器反复重启。
- 登录/权限隔离异常。
- 新写入失败、迁移异常或数据数量突变。
- 学生提交、教师审核等核心流程阻断。
- AI 配置无法解密且影响现网功能。

代码镜像可以回退，数据库结构不一定能安全反向迁移。涉及 0040/0041 的回滚应优先使用发布前完整备份在维护窗口恢复，不能盲目执行 `migrate core 0039`。

## 11. 运维与故障处理

| 症状 | 第一检查 | 正常现象 | 下一步 |
| --- | --- | --- | --- |
| 网页打不开 | `docker compose ps`、Nginx 日志 | nginx Up，80 端口监听 | 检查云防火墙/DNS；勿先重启数据库 |
| API 失败 | `/api/health/`、backend 日志 | backend healthy | 查数据库/Redis连接、磁盘、内存 |
| 无法登录 | backend 日志、浏览器网络请求 | `/api/csrf/` 与登录接口可达 | 确认账号状态和 Cookie/CSRF 域名 |
| AI 不响应 | celery 状态、AI 可用性、celery 日志 | worker Up，队列可消费 | 核对 Key 是否配置，不在日志打印 Key |
| 上传不可用 | 健康接口 capabilities | 生产当前为 false | 这是功能开关，不是重启能解决 |
| PDF 不可用 | 健康接口 capabilities | 生产当前为 false | 需部署 Gotenberg 并评估内存 |
| 磁盘告警 | `df -h`、Docker 占用 | 当前根盘 31% | 先定位，不删除卷或备份 |
| 任务积压 | celery 日志、Redis 状态 | worker 正常消费 | 查外部 AI/转换依赖和失败重试 |

常用只读命令：

```bash
# [只读]
ssh ubuntu@1.15.230.239 'docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Image}}"'
ssh ubuntu@1.15.230.239 'docker logs --since 30m --tail 200 lingsu-celery-1'
ssh ubuntu@1.15.230.239 'cd /home/ubuntu/lingsu && docker compose exec -T backend python manage.py showmigrations --plan'
```

不要通过反复重启掩盖问题。先保存时间、症状、请求路径、容器状态和相关日志，再决定变更。

## 12. 质量与风险报告

### 12.1 2026-08-31 实测质量基线

| 检查 | 结果 |
| --- | --- |
| `npm test` | 67 文件、325 测试全部通过 |
| `npm run build` | 通过；主包 928.68KB，存在体积警告 |
| `manage.py check` | 通过，0 issue |
| `makemigrations --check --dry-run` | 通过，无变化 |
| 后端测试 | 253 通过，1 跳过，43.035 秒 |
| Playwright E2E | 非全绿；观察到 6 组失败并人工终止，不能作为发布门禁通过 |
| 线上容器 | 5 个运行服务，均 0 重启 |
| 线上近 24h错误关键词 | backend/celery/postgres/redis 为 0；nginx 命中 1，需结合原日志判断 |

### 12.2 CI 能做什么

`mvp.yml` 在 `main`、`codex/**` push 和 PR 时运行：前端安装、单测、构建；后端安装、检查、迁移和测试。CI 当前不运行 Playwright E2E，也不验证 Docker Compose 生产部署。

`publish-images.yml` 在 `main` 和 `codex/ui-audit-consistency` push 或手动触发时向 GHCR 发布以完整 Git SHA 标记的后端/Web 镜像。

### 12.3 优先级

- P0：建立并验证生产备份；完成异地副本；确认所有权。
- P0：补 HTTPS、真实域名和证书治理。
- P0：统一“仓库提交—镜像 SHA—部署记录”，消除不可追溯漂移。
- P1：修复 E2E 基线并加入 CI。
- P1：拆分 `views.py`、`tasks.py`，降低回归风险。
- P1：前端代码分包，降低首屏负担。
- P1：决定是否正式启用附件和 PDF，并准备相应安全/资源能力。
- P2：监控、告警、备份成功通知和恢复演练常态化。

## 13. 资产与权限移交

| 资产 | 当前入口/位置 | 当前所有者 | 新所有者 | 轮换动作 | 验收证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 云服务器 | `1.15.230.239` | 待确认 | 待指定 | 移交云控制台、账单、实名与安全组 | 新负责人可登录控制台 | 待办 |
| SSH | `ubuntu@1.15.230.239` | 待确认 | 待指定 | 新增新负责人公钥，确认后删除旧公钥 | 新钥匙登录成功 | 待办 |
| Git 仓库 | 当前 remote/组织 | 待确认 | 待指定 | 转移组织管理权、分支保护和恢复码 | 新负责人可管理仓库 | 待办 |
| GHCR 镜像 | `ghcr.io/aanzhi/*` | 待确认 | 待指定 | 移交 packages 权限和发布工作流 | 可拉取指定 SHA | 待办 |
| 域名/DNS | 线上未识别正式域名 | 待确认 | 待指定 | 确认注册商、DNS、续费与解析 | 管理台截图/记录 | P0 |
| HTTPS 证书 | 未发现 | 待确认 | 待指定 | 配置证书和续期责任 | HTTPS 检查通过 | P0 |
| Django 密钥 | 线上 `.env` | 原服务器管理员 | 新服务器管理员 | 维护窗口轮换并验证会话影响 | 新配置生效 | 待办 |
| 数据库口令 | 线上 `.env` | 原服务器管理员 | 新服务器管理员 | 备份后轮换并更新服务 | 后端健康 | 待办 |
| AI 服务 Key | 线上 `.env` / 后续平台配置 | 待确认 | 待指定 | 撤销旧 Key、创建限额新 Key | AI 冒烟通过 | 待办 |
| AI 配置加密密钥 | 生产尚未配置 | 无 | 新服务器管理员 | 升级前生成并异地保管 | 加解密测试通过 | P0（升级前） |
| 生产数据库 | Docker `postgres_data` | 待确认 | 新运维负责人 | 建备份、加密、异地保存和恢复演练 | 最近备份可恢复 | P0 |
| 媒体数据 | Docker `media_data` | 待确认 | 新运维负责人 | 与数据库同步备份 | 文件校验通过 | P0 |
| Redis | Docker `redis_data` | 待确认 | 新运维负责人 | 明确仅作队列/缓存 | 服务健康 | 待办 |
| systemd 服务 | lingsu / lingsu-console | 服务器管理员 | 新运维负责人 | 移交启停与日志权限 | 能只读查看状态 | 待办 |
| 监控告警 | 未发现项目级系统 | 无/待确认 | 新运维负责人 | 配置可用性、磁盘和备份告警 | 告警演练 | P1 |
| 业务账号 | 三类角色 | 各账号持有人 | 新业务负责人 | 禁用离职账号、轮换管理员 | 权限复核记录 | P0 |

## 14. 接手验收清单

### 14.1 业务负责人

- [ ] 知道学生、教师、平台三类入口和职责。
- [ ] 能使用三类测试账号完成登录。
- [ ] 理解线上附件和 PDF 当前关闭。
- [ ] 知道哪些数据是真实数据，禁止用于测试。
- [ ] 已确认域名、服务器、仓库和第三方服务的新所有者。

### 14.2 开发负责人

- [ ] 能在本地启动集成环境并读取健康接口。
- [ ] 能运行前后端测试和构建。
- [ ] 知道核心模型、工作流和权限代码位置。
- [ ] 理解生产只到迁移 0039、本地到 0041。
- [ ] 理解运行镜像与服务器 Git 工作区不是同一版本。
- [ ] 已记录 E2E 失败并制定修复计划。

### 14.3 运维负责人

- [ ] 使用自己的 SSH Key 登录成功。
- [ ] 能只读查看容器、日志、磁盘和内存。
- [ ] 已创建数据库、媒体和环境快照备份。
- [ ] 已把备份复制到服务器外并验证可读。
- [ ] 已记录当前镜像 SHA 和回滚材料。
- [ ] 已配置 HTTPS、监控、磁盘和备份告警，或有明确期限。
- [ ] 已完成旧账号、旧 Key 和旧 SSH 公钥轮换。

签收记录：

| 角色 | 姓名 | 日期 | 结论 | 未完成项 |
| --- | --- | --- | --- | --- |
| 业务负责人 |  |  |  |  |
| 开发负责人 |  |  |  |  |
| 运维负责人 |  |  |  |  |

## 15. 附录

### 15.1 命令速查

```bash
# [只读] 本地状态
docker compose --env-file .env.integration ps

# [只读] 线上状态
ssh ubuntu@1.15.230.239 'cd /home/ubuntu/lingsu && docker compose ps'

# [只读] 线上健康
ssh ubuntu@1.15.230.239 'curl -fsS http://127.0.0.1/api/health/'

# [只读] 线上迁移
ssh ubuntu@1.15.230.239 'cd /home/ubuntu/lingsu && docker compose exec -T backend python manage.py showmigrations --plan'

# [只读] Git 差异
git log --oneline 620aa599bc224bb0a9e9ff72d8fe89a2ac0a3cab..HEAD
git diff --stat 620aa599bc224bb0a9e9ff72d8fe89a2ac0a3cab..HEAD
```

### 15.2 术语

- **迁移（migration）**：数据库结构的版本变更文件。
- **镜像（image）**：构建完成、用于启动容器的不可变软件包。
- **容器（container）**：由镜像启动的运行实例。
- **命名卷（volume）**：Docker 管理的持久数据目录。
- **Celery**：在后台执行 AI、扫描和报告任务的队列 worker。
- **烟雾测试**：发布后快速检查最关键功能是否可用。
- **回滚**：升级失败后恢复到已知可用版本与数据状态。

### 15.3 文档依据

- `README.md`
- `docs/项目运行说明.md`
- `deploy/README.md`
- `docker-compose.yml`
- `.github/workflows/mvp.yml`
- `.github/workflows/publish-images.yml`
- 当前前后端源码、测试和数据库迁移
- 2026-08-31 本地命令实测与生产服务器只读审计

### 15.4 文档核验记录

| 项目 | 日期 | 结果 |
| --- | --- | --- |
| 本地静态分析 | 2026-08-31 | 完成 |
| 前端单测与构建 | 2026-08-31 | 单测/构建通过，存在包体积警告 |
| 后端检查与测试 | 2026-08-31 | 通过，1 个测试跳过 |
| 浏览器 E2E | 2026-08-31 | 未通过，已列为 P1 |
| 生产只读审计 | 2026-08-31 | 完成，未修改服务和数据 |
| 双环境差异 | 2026-08-31 | 完成，禁止直接升级 |
| 敏感信息扫描 | 2026-08-31 | 通过；未发现明文密码、Key、Token、私钥或数据库连接串 |

## 16. 可照做的操作手册

本章面向第一次接手项目的人。命令在哪台机器执行、执行前检查什么、成功时看到什么、失败时如何停止，均写在步骤中。

### 16.1 从零启动本地测试环境

**执行人：** 开发负责人。

**执行位置：** 本地 Mac 的“终端”应用。
**影响：** 只改变本地 Docker/Colima 和本地集成数据，不影响线上。

1. 进入项目目录：

   ```bash
   cd /Users/anzhi/Desktop/雷灵/星辰/lingsu
   pwd
   ```

   成功标准：最后一行正好是 `/Users/anzhi/Desktop/雷灵/星辰/lingsu`。如果目录不存在，先确认项目是否被移动，不要新建空目录冒充项目。

2. 检查 Colima 和 Docker：

   ```bash
   # [只读]
   colima status
   docker version
   ```

   成功标准：Colima 显示 running，`docker version` 同时显示 Client 和 Server。若 Colima 未运行：

   ```bash
   # [变更] 只启动本地 Docker 虚拟机
   colima start
   ```

3. 检查本地集成配置文件：

   ```bash
   # [只读] 只确认文件存在，不打印内容
   test -f .env.integration && echo 'integration config exists'
   ```

   成功标准：输出 `integration config exists`。如果没有该文件，从负责人处取得本地测试配置；不要复制线上 `.env`。

4. 启动本地服务：

   ```bash
   # [变更] 只作用于本地
   docker compose --env-file .env.integration --profile dev up --build -d
   ```

   第一次可能需要数分钟下载镜像和安装依赖。命令返回终端提示符后继续。

5. 检查容器：

   ```bash
   # [只读]
   docker compose --env-file .env.integration ps
   ```

   成功标准：backend、postgres、redis 显示 `healthy`；frontend、celery、celery_beat、clamav、gotenberg 显示 `Up` 或 healthy。

6. 检查网页和 API：

   ```bash
   # [只读]
   curl -fsS http://127.0.0.1:18001/api/health/
   curl -fsSI http://127.0.0.1:5173/ | head
   ```

   成功标准：API 返回 `status` 为 `ok`，前端响应包含 `200 OK`。然后浏览器打开 `http://127.0.0.1:5173`。

7. 如果启动失败，只查看日志，不重建数据：

   ```bash
   # [只读]
   docker compose --env-file .env.integration logs --tail 150 backend
   docker compose --env-file .env.integration logs --tail 150 frontend
   ```

   把第一条明确的 error、执行时间和容器状态记录下来。不要运行 `down -v`。

### 16.2 初始化本地演示数据

**前置条件：** 已确认连接的是 `.env.integration` 本地数据库；浏览器中没有需要保留的本地测试数据。

1. 先检查当前数据库名，只输出名称：

   ```bash
   # [只读]
   docker compose --env-file .env.integration exec -T postgres sh -lc 'printf "%s\n" "$POSTGRES_DB"'
   ```

   如果输出像生产库或无法确认，立即停止。

2. 播种演示数据：

   ```bash
   # [变更] 仅本地
   docker compose --env-file .env.integration exec -T backend python manage.py seed_demo
   docker compose --env-file .env.integration exec -T backend python manage.py seed_ai_agents
   ```

3. 成功标准：命令明确提示演示学校、账号或 Skills 已创建；使用 `docs/项目运行说明.md` 中的演示账号登录。

4. 只有明确要清空并重建演示数据时才使用 `--reset`。生产服务器永远禁止执行。

### 16.3 每次接手代码后的本地验收

**执行人：** 开发负责人。
**执行时机：** 拉取代码后、准备发布前、合并重要改动前。

1. 确认代码状态：

   ```bash
   # [只读]
   git status --short
   git branch --show-current
   git log -5 --oneline
   ```

   如果存在不认识的本地修改，先确认归属，不要覆盖或丢弃。

2. 前端测试和构建：

   ```bash
   cd frontend
   npm ci
   npm test
   npm run build
   cd ..
   ```

   成功标准：测试 0 failed；`npm run build` 退出码为 0。包体积 warning 可以记录，但 error 不能忽略。

3. 后端检查：

   ```bash
   docker compose --env-file .env.integration exec -T backend python manage.py check
   docker compose --env-file .env.integration exec -T backend python manage.py makemigrations --check --dry-run
   docker compose --env-file .env.integration exec -T backend python manage.py test apps.core.tests --keepdb
   ```

   成功标准：系统检查 0 issue、`No changes detected`、测试 0 failed。

4. E2E 当前不是稳定基线。运行前确认无人同时重置 E2E 数据：

   ```bash
   cd frontend
   npm run test:e2e
   ```

   成功标准：25 项全部通过且进程自行退出。若出现 `seed_core_e2e --reset` 冲突，先停止并确认没有第二个 Playwright 进程；不要并发重复运行。

### 16.4 线上每日巡检

**执行人：** 运维负责人。

**执行位置：** 任意已配置新负责人 SSH Key 的安全电脑。
**影响：** 以下全部只读。

1. 登录并确认身份：

   ```bash
   ssh ubuntu@1.15.230.239
   id
   hostname
   date -Is
   ```

   成功标准：用户为 `ubuntu`，主机名为 `VM-0-12-ubuntu`，时间正确。

2. 进入部署目录并检查服务：

   ```bash
   cd /home/ubuntu/lingsu
   docker compose ps
   ```

   成功标准：backend、postgres、redis healthy；celery、nginx Up。任何容器 Restarting、Exited 或 unhealthy 都是异常。

3. 检查 API：

   ```bash
   curl -fsS http://127.0.0.1/api/health/
   ```

   当前正常结果：`status=ok`；附件和 PDF 为 false 是当前生产配置，不需要为此重启。

4. 检查资源：

   ```bash
   df -h /
   free -h
   swapon --show
   ```

   处理标准：根盘超过 75% 开始排查，超过 85% 立即告警；不要直接删除 Docker 卷。

5. 检查最近错误：

   ```bash
   docker logs --since 24h --tail 500 lingsu-backend-1 2>&1 | grep -Ei 'error|exception|critical|fatal' | tail -30
   docker logs --since 24h --tail 500 lingsu-celery-1 2>&1 | grep -Ei 'error|exception|critical|fatal' | tail -30
   ```

   没有输出表示这段日志未命中关键词，不等于系统绝对无问题。出现错误时记录时间、容器、错误摘要；日志若含用户内容必须脱敏。

6. 退出服务器：

   ```bash
   exit
   ```

### 16.5 第一次建立生产备份

**执行人：** 新运维负责人。
**风险：** `[高风险]`。虽然备份不应修改业务数据，但会读取全库、占用磁盘并保存敏感环境快照。必须先获得新负责人批准，在低峰期执行。

1. 登录后检查预计空间：

   ```bash
   ssh ubuntu@1.15.230.239
   cd /home/ubuntu/lingsu
   df -h /
   docker compose exec -T postgres sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -Atc "select pg_size_pretty(pg_database_size(current_database()));"'
   ```

2. 创建只允许当前用户访问的备份根目录：

   ```bash
   # [变更]
   install -d -m 700 /home/ubuntu/backups
   ```

3. 执行项目备份脚本：

   ```bash
   # [高风险]
   BACKUP_ROOT=/home/ubuntu/backups BACKUP_RETENTION_DAYS=30 ./scripts/backup.sh
   ```

   成功标准：最后输出 `Backup created:` 和时间戳目录，没有 error。

4. 检查备份，不读取其中的密钥值：

   ```bash
   latest=$(find /home/ubuntu/backups -mindepth 1 -maxdepth 1 -type d | sort | tail -1)
   test -n "$latest"
   ls -lh "$latest"
   docker compose exec -T postgres pg_restore --list < "$latest/database.dump" | head
   test -s "$latest/media.tar.gz"
   test -s "$latest/environment.snapshot"
   ```

   成功标准：三个文件均非空，容器内的 `pg_restore --list` 能列出数据库对象。

5. 把整个时间戳目录复制到服务器之外的加密存储。只有服务器本机副本不算灾备。

6. 不要在生产直接运行 `restore.sh` 验证恢复。应在隔离服务器或隔离数据库演练。

### 16.6 生产发布操作

**执行人：** 开发负责人和运维负责人共同执行。
**风险：** `[高风险]`。当前版本漂移和迁移 0040/0041 未经过生产副本演练，因此在第 10 节清单全部完成前禁止执行。

1. 在发布单中记录：旧后端镜像 SHA、旧 Web 镜像 SHA、新 Git SHA、备份目录、维护窗口和回滚负责人。

2. 确认新镜像已由 GitHub Actions 发布，标签必须是完整提交 SHA，不能写 `latest`。

3. 在服务器只修改 `.env` 中的 `BACKEND_IMAGE`、`WEB_IMAGE` 以及本次明确新增的配置；先备份旧配置：

   ```bash
   # [高风险]
   cd /home/ubuntu/lingsu
   cp .env ".env.before-release-$(date +%Y%m%dT%H%M%S)"
   chmod 600 .env.before-release-*
   ```

4. 本地最新版升级还必须配置 `AI_CONFIG_ENCRYPTION_KEY`。密钥应由密码管理器生成和保存，不得写入 Git、聊天或发布单。

5. 拉取固定镜像：

   ```bash
   # [高风险]
   docker compose --env-file .env --profile production --profile async pull backend celery nginx
   ```

6. 先确认备份和维护窗口，再更新服务：

   ```bash
   # [高风险]
   docker compose --env-file .env --profile production --profile async up -d --no-build postgres redis backend celery nginx
   ```

   backend 启动命令会自动执行迁移，所以这一步已经包含数据库变更，不能当成普通重启。

7. 逐项验收：

   ```bash
   docker compose ps
   curl -fsS http://127.0.0.1/api/health/
   docker compose exec -T backend python manage.py showmigrations --plan | tail -15
   docker logs --since 10m --tail 300 lingsu-backend-1
   docker logs --since 10m --tail 300 lingsu-celery-1
   ```

   然后由业务负责人实际验证：三角色登录、学生查看项目、教师查看指导项目、平台查看学校、AI 发起一次受控请求。

8. 任一 P0 功能失败，立即停止业务写入并进入回滚判断，不要连续反复发布。

### 16.7 回滚操作

1. **仅前端异常且数据库/后端正常：** 把 `WEB_IMAGE` 改回发布单记录的旧 SHA，执行 Nginx 的 `up -d --no-build`，再验证页面。

2. **后端异常但没有执行新迁移或新数据写入：** 把 `BACKEND_IMAGE` 改回旧 SHA，更新 backend/celery，再验证健康和权限。

3. **已经应用 0040/0041 或产生新数据：** 不要直接降级迁移。进入维护窗口，在隔离环境先验证发布前备份；由数据库负责人决定恢复整个数据库和媒体，而不是只回退镜像。

4. 恢复脚本会清理现有数据库对象、删除媒体内容并重启服务：

   ```bash
   # [高风险] 仅展示，未经批准禁止在生产执行
   backup_dir=$(find /home/ubuntu/backups -mindepth 1 -maxdepth 1 -type d | sort | tail -1)
   test -n "$backup_dir"
   ./scripts/restore.sh "$backup_dir"
   ```

5. 回滚后必须再次检查容器、健康接口、迁移状态、三角色登录和核心业务数据；记录原因和受影响时间段。

### 16.8 常见故障的明确处理顺序

#### 网页打不开

1. 从外部浏览器确认是否所有人都打不开。
2. SSH 登录，执行 `docker compose ps`。
3. Nginx 不在 Up：查看 `docker logs --tail 200 lingsu-nginx-1`。
4. Nginx 正常：执行 `curl -I http://127.0.0.1/`。
5. 本机能访问、外部不能访问：检查云安全组、防火墙和域名，不要重启数据库。

#### API 或登录失败

1. 执行 `curl -fsS http://127.0.0.1/api/health/`。
2. 失败时检查 backend、postgres、redis 是否 healthy。
3. 查看 backend 最近 200 行日志。
4. 如果出现数据库连接错误，先检查 PostgreSQL 状态和磁盘；不要修改数据库密码碰运气。
5. 如果只出现 CSRF/403，核对访问域名是否在允许域名和 CSRF 来源配置中。

#### AI 一直排队

1. 检查 `lingsu-celery-1` 是否 Up。
2. 查看 Celery 日志是否有认证、限流、网络或模型错误。
3. 在平台状态页确认 AI 是否配置和可用。
4. 认证错误时轮换/修复 Key；不要把 Key 打印到终端记录或工单。
5. 保留失败的 AI 日志用于审计，不直接改数据库状态为 completed。

#### 磁盘不足

1. 执行 `df -h /` 和 `docker system df`。
2. 找出增长来源：容器日志、旧镜像、备份或媒体。
3. 先转移备份、设置日志轮转；确认镜像不再用于回滚后再清理旧镜像。
4. 永远不要删除 `postgres_data`、`media_data`，也不要运行 `docker compose down -v`。

---

本手册记录的是 2026-08-31 的可验证状态。接手后每次生产发布都应更新第 1.3 节版本基线、第 9 节差异和第 15.4 节核验日期。
